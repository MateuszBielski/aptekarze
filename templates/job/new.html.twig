{% extends 'base.html.twig' %}

{% block title %}Nowe stanowisko{% endblock %}

{% block body %}
    <h3>{{ title|default("Utwórz nowe stanowisko") }}</h3>

    {{ include('job/_form.html.twig') }}
{% endblock %}
{% block belka_dol %}
    <div class="pole_belka_dol">
        <a href="{{ path('job_index') }}">powrót do listy</a>
    </div>
    <div class="pole_belka_dol">
        <label for="submit-form" class="submit_label">zatwierdź zmiany</label>{# tabindex="0" #}
    </div>
{% endblock %}