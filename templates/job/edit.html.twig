{% extends 'base.html.twig' %}

{% block title %}Edit Job{% endblock %}

{% block body %}
    <h3>Edytuj stanowisko</h3>

    {{ include('job/_form.html.twig', {'button_label': 'Update'}) }}


    {{ include('job/_delete_form.html.twig') }}
{% endblock %}

{% block belka_dol %}
    <div class="pole_belka_dol">
        <a href="{{ path('job_index') }}">powrót do listy</a>
    </div>
    <div class="pole_belka_dol">
        <label for="submit-form" class="submit_label">zatwierdź zmiany</label>{# tabindex="0" #}
    </div>
    <div class="pole_belka_dol">
            <label for="submit-delete" class="submit_label">usuń stanowisko</label>
    </div>
{% endblock %}
