{% extends 'base.html.twig' %}

{% block title %}Edytuj historyczną zmianę danych{% endblock %}

{% block body %}
    <h3>Edytuj historyczną zmianę danych</h3>

    {{ include('member_history/_form.html.twig', {'button_label': 'Update'}) }}

    {# <a href="{{ path('member_history_index') }}">back to list</a> #}

    {{ include('member_history/_delete_form.html.twig') }}
{% endblock %}
{% block belka_dol %}   
    <div class="pole_belka_dol">
        <a href="{{ app.request.headers.get('referer') }}">powrót</a>
    </div>
    <div class="pole_belka_dol">
        <label for="submit-form" class="submit_label">Zatwierdź zmianę</label>
    </div>
    <div class="pole_belka_dol">
        <label for="submit-delete" class="submit_label">usuń zmianę</label>
    </div>
{% endblock %}