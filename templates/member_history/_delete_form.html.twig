<form method="post" action="{{ path('member_history_delete', {'id': member_history.id}) }}" onsubmit="return confirm('Czy napewno usunąć zmianę?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ member_history.id) }}">
    {# <button class="btn">Delete</button> #}
    <input type="submit" id="submit-delete" class="hidden" />
</form>
