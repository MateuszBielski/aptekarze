{% extends 'base.html.twig' %}

{% block title %}wpłata składki{% endblock %}

{% block body %}
    <h3>nowa wpłata składki</h3>

    {{ include('contribution/_form.html.twig') }}

    {# <a href="{{ path('contribution_index') }}">powrót do listy</a> #}
{% endblock %}
{% block belka_dol %}
    <div class="pole_belka_dol">
        <a href="{{ path('contribution_index') }}">rezygnuj</a>
    </div>
    <div class="pole_belka_dol">
        <label for="submit-form" class="submit_label">zapisz</label>{# tabindex="0" #}
    </div>
{% endblock %}
