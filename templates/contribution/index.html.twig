{% extends 'base.html.twig' %}

{% block title %}zapłacone składki{% endblock %}

{% block body %}
    <h1>zapłacone składki</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Imię i Nazwisko</th>
                <th>Kwota</th>
                <th>Data wpłaty</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for contribution in contributions %}
            <tr>
                <td>{{ contribution.id }}</td>
                {% set mu = contribution.myUser %}
                <td>{{ mu.firstName }} {{ mu.surname }}</td>
                <td>{{ contribution.value }}</td>
                <td>{{ contribution.paymentDate ? contribution.paymentDate|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('contribution_show', {'id': contribution.id}) }}">show</a>
                    <a href="{{ path('contribution_edit', {'id': contribution.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('contribution_new') }}">Create new</a>
{% endblock %}
